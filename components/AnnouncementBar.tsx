/** Sitewide announcement strip, fixed above the header. Static, no JS. */
export default function AnnouncementBar() {
  return (
    <div className="announcement-bar" role="note">
      <span>
        <strong>50%</strong> of all proceeds go towards medical research
      </span>
    </div>
  );
}
