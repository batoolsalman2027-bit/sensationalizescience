# dependencies
node_modules

# next.js build output
.next
out
*.tsbuildinfo
next-env.d.ts

# environment variables / secrets (never commit real values)
.env
.env.local
.env.*.local
.env.development
.env.production
*.pem
*.key

# runtime data (SQLite DB + generated projects/figures)
data/

# generated media (rendered audio + video, can be hundreds of MB)
public/renders/

# logs
*.log
npm-debug.log*

# OS / editor cruft
.DS_Store
.vscode/
.idea/

# local agent tooling
.claude/
# local agent skill copies (optional — uncomment to keep them out of Git)
# .agents/

# keep the example env template tracked
!.env.example
.manim-venv/
poc/out/
