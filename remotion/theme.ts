/**
 * Color/font constants mirroring app/globals.css. Remotion compositions
 * render in an isolated bundle and can't read Next's CSS variables, so the
 * palette is duplicated here as plain values.
 */
export const theme = {
  bg: "#0b0d12",
  panel: "#151922",
  border: "#262c3a",
  text: "#e8eaf0",
  muted: "#8b93a7",
  accent: "#6b8afd",
  fontFamily:
    'ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, Helvetica, Arial, sans-serif',
};
